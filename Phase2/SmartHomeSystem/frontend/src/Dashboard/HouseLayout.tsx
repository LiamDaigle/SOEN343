import React from "react";
import "./HouseLayout.css"

const HouseLayout = () => {
  return (
    <div className="house-layout-container">
      <h1>Inert House Layout here</h1>{" "}
      {/* TODO: put the house layout (we can refactor the files later) */}
    </div>
  );
};

export default HouseLayout;
